## Nuxt 多端 API 地址环境变量示例
# 本地开发无需设置此变量，两个 app 均已内置本地默认值：
#   admin: /mortise（相对路径，Vite proxy 转发至 localhost:9999，规避 CORS）
#   web:   http://localhost:9999/mortise（直连）
#
# 仅在需要指向非 localhost 后端时（如测试/生产环境）才设置：
# NUXT_PUBLIC_API_BASE=https://api.your-domain.com/mortise

# 如需区分多端，可单独指定（一般只需全局 NUXT_PUBLIC_API_BASE 即可）
# NUXT_PUBLIC_API_BASE_ADMIN=https://api.your-domain.com/mortise
# NUXT_PUBLIC_API_BASE_WEB=https://api.your-domain.com/mortise
# NUXT_PUBLIC_API_BASE_SITE=https://api.your-domain.com/mortise
